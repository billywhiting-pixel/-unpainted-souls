function startVisualizer(engine){let cv=document.getElementById('visualizer'),c=cv.getContext('2d'),stars=[];function size(){cv.width=innerWidth;cv.height=innerHeight;stars=Array.from({length:80},()=>({x:Math.random()*cv.width,y:Math.random()*cv.height,r:Math.random()*1.7+.3,v:Math.random()*.35+.05}))}addEventListener('resize',size);size();function draw(){c.clearRect(0,0,cv.width,cv.height);let level=0;if(engine.analyser){let data=new Uint8Array(engine.analyser.frequencyBinCount);engine.analyser.getByteFrequencyData(data);level=data.reduce((a,b)=>a+b,0)/data.length}c.fillStyle='rgba(255,224,138,.55)';stars.forEach(s=>{s.y+=s.v+level/900;if(s.y>cv.height)s.y=0;c.beginPath();c.arc(s.x,s.y,s.r,0,Math.PI*2);c.fill()});let cx=cv.width/2,cy=cv.height/2.7,r=44+level*.55,g=c.createRadialGradient(cx,cy,5,cx,cy,r*2.4);g.addColorStop(0,'rgba(255,224,138,.9)');g.addColorStop(.35,'rgba(131,217,255,.25)');g.addColorStop(1,'rgba(255,224,138,0)');c.fillStyle=g;c.beginPath();c.arc(cx,cy,r*2.4,0,Math.PI*2);c.fill();requestAnimationFrame(draw)}draw()}window.startVisualizer=startVisualizer;